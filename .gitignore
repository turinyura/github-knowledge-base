# Examples 1 and 2 are just documentation - only the actual input matters
# No build artifacts, dependency directories, or temporary files in the changes
# Only a markdown documentation file was added
# Therefore, no ignores are needed